#include <stdio.h>
#include <string.h>
/*
 * main: Entry point
 * Description: Prints with proper grammar, but the outcome is a piece of art, using printf
 * Return:  0
 */
int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,"; 
	printf("%s\n", str);
	return (0);
}
