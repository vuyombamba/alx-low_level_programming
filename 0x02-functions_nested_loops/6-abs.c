#include "main.h"

/**
 * _abs - it calculates the absolute value of the parameter
 * @i: parameter to intake a value
 * Return: Always the size of int
 */
int _abs(int i)
{
	return (i < 0 ? -i : i);
}
